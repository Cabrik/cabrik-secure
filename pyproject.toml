[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "cabrik-secure"
version = "1.0.0"
description = "Cabrik Secure"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["cabrik_secure*"]
